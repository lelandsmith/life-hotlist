{
  "name": "hotlist",
  "version": "1.0.0",
  "description": "Life Hotlist - A client relationship management tool for tracking leads and conversions",
  "main": "index.js",
  "scripts": {
    "dev": "live-server --host=localhost --port=3000",
    "start": "live-server --host=localhost --port=8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
